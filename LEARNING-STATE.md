# Learning State

Updated: 2026-08-17 — current synthesis after first learning sessions; no active session.

## Near-term direction

Build a clear foundation for describing location, residence, and origin in simple Life/Work contexts, while gradually opening listening, speaking, and repair evidence. Do not advance to the original `live in / be from` contrast until the underlying `be` mental model is acquired without copying.

## Current bounded objective

**Acquire the mental model of English verbs and `be`**: understand that ordinary complete English sentences need a verb; recognize action verbs versus linking `be`; understand that `am/is/are` are subject-dependent forms. Current status: conceptual understanding is independent in familiar examples; `am/is/are` production is independent familiar in a small closed-book check; retention and flexible transfer remain provisional.

## Active evidence

- Reading gist/detail: short Life/Work message understood; well-supported, familiar slice.
- Verb mental model: correctly classified `works/lives` as actions and `is` as linking description; independent familiar, retention untested.
- Subject forms: `I am`, one person `is`, `you/we/they are` produced without visible rule in simple sentences; independent familiar, retention untested.
- `be in` versus `live in`: distinction understood conceptually; production contrast is not yet independently acquired.
- Earlier `live in / be from` outputs were open-reference/guided pattern matching and are not mastery evidence.

## Active blockers and weaknesses

- Foundational acquisition blocker for `live in / be from`: do not test or delay-review the contrast yet.
- Important weakness: transfer the `be` mental model to new contexts without copying examples.
- Text-only session evidence does not establish speaking, listening, pronunciation, or live repair ability.

## Review and delayed needs

- No delayed retrieval until meaningful acquisition and a closed-book comprehension/production check exist.
- Later review should test `am/is/are` and `be in` versus `live in` with changed prompts, not rereading the rule.

## Modality and coverage gaps

- Listening gist/detail/parsing/response-readiness: unknown/not tested; no actionable voice assignment currently active.
- Speaking production/interaction/repair and pronunciation: unknown/not tested; no negative evidence inferred.

## Next action

At the next explicitly started session, announce one human-readable goal and continue acquisition with one meaningful contextual example of `be in` versus `live in`. Ask one sufficient comprehension check, then either continue with useful practice or end the session explicitly. Do not start a new objective automatically.

## Session status

No session is active. No voice assignment is currently actionable. When one becomes pedagogically appropriate, create the self-contained handoff at `external-practice/voice/current.md`; import a returned `external-practice/voice/result.md` into this synthesis, then remove or replace both ephemeral files. Neither is source of truth.
