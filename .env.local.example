# Provider-independent manual bridge.
# Open current.md in a voice-capable client, save its VOICE_RESULT to result.md.
VOICE_PROVIDER_COMMAND=node tools/voice/manual-provider.mjs

# Optional: shorten the manual wait during testing (milliseconds).
# VOICE_MANUAL_TIMEOUT_MS=1800000

OPENAI_API_KEY=твой_ключ
